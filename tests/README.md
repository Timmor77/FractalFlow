# Tests

Add tests alongside stable behavior rather than speculative APIs. Start with
pure viewport-coordinate tests, then shader compilation/browser smoke tests and
Python reference-image checks.

Generated images and measurements belong in `artifacts/`, not in this folder.

